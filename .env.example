# Paystack Configuration

# Paystack Public Key (Required)
# Get your public key from: https://dashboard.paystack.com/#/settings/keys
# Test mode key (starts with pk_test_)
VITE_PAYSTACK_PUBLIC_KEY=pk_test_your_key_here

# Paystack Live Key (Optional - for production)
# Live mode key (starts with pk_live_)
# Only use this when you're ready to accept real payments
VITE_PAYSTACK_LIVE_KEY=pk_live_your_key_here

# Notes:
# 1. The app automatically uses test key in development mode
# 2. The app uses live key in production mode (if provided)
# 3. Never commit this file to version control
# 4. Create a copy named ".env" from this template
